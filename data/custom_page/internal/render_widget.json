{
    "controller": "widgets.start.type = args.widget_type; widgets.start.settings = JSON.parse(args.input); widgets[args.widget_id] = widgets.start; delete widgets.start; slotAssignments['body'] = [args.widget_id]; callback(); ",
    "code": {
        "widgets": {
            "start": {
                "settings": {},
                "widget": "render_widget"
            }
        },
        "slotAssignments": {
            "body": [
                "start"
            ]
        }
    }
}